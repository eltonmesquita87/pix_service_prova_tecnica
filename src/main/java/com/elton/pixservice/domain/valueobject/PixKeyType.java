package com.elton.pixservice.domain.valueobject;

/**
 * Enum representing the types of Pix keys supported by the system.
 */
public enum PixKeyType {
    CPF,
    EMAIL,
    PHONE,
    EVP  // Chave aleatória (Endereço Virtual de Pagamento)
}
